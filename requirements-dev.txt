# Python dependencies for the TOAP benchmarks and paper figures.
# (The Rust workspace itself needs only a cargo toolchain.)
tiktoken>=0.7      # tokenizer-accurate token counts (benchmark/*)
matplotlib>=3.8    # paper figures (paper/figures/gen_*.py)
